# .nosana-ci.yml
global:
  image: nosana/solana
  trigger:
    push:
      branches:
        - "*"

jobs:
  - name: build
    commands:
        - npm ci
        - npm run build:program-rust
    artifacts:
      - name: target

  - name: test
    commands:
      - npm run test:program-rust
    resources:
      - name: target

  - name: deploy
    commands:
      - echo "deploying..."
    resources:
      - name: target